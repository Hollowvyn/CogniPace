// Overlay-session keeps its view models colocated in ui/screens/types/
// for now (Phase 5 split). A future cleanup may promote them into this
// folder; until then this barrel exists to satisfy the "every feature
// has domain/" rule.
export {};
